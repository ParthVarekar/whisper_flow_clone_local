# Second Brain — environment configuration
# Copy this file to `.env` and edit as needed.

# SQLite database location. Relative paths resolve from the prisma/ directory
# (Prisma quirk), so prefer an absolute path or a path relative to the project
# root as shown.
DATABASE_URL="file:/home/z/whisper_flow_clone/second-brain/db/second-brain.db"
